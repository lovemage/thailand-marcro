(function(){
  // Stub for Canvas headers module to silence 404 and avoid errors.
  // If the theme expects initialization here, keep it no-op.
  function ready(fn){ if(document.readyState!=='loading'){ fn(); } else { document.addEventListener('DOMContentLoaded', fn);} }
  ready(function(){ /* no-op */ });
})();

